apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oidc.fullname" . }}-test-connection"
  labels:
    {{- include "oidc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oidc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
